using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Sasha : Dresseur
{
    // Start is called before the first frame update
    void Start()
    {
        nameOfPokemon = "PokemonDeSasha";
        GetTeam();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
