using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ondine : Dresseur
{
    // Start is called before the first frame update
    void Start()
    {
        nameOfPokemon = "PokemonD'Ondine";
        GetTeam();
    }

    // Update is called once per frame
    void Update()
    {

    }
}
